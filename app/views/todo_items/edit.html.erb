<%= form_for [@todo_list, @todo_item] do |form| %>
<% if @todo_item.errors.any? %>
<div class="error alert">
  <h2><%= pluralize(@todo_item.errors.count, "error") %> prohibited this todo item from being saved:</h2>

  <ul>
    <% @todo_item.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>
<div class = "item">
<div class = "create">
            <div class="form-heading">
              <h2 class="header"><%= @todo_list.title %> - Edit Todo List Item</h2>
              <hr>
            </div>
            <div class="form-body">
            <form class="form-inline">
                <div class="form-group">
                      <div class="input-group horz">
                        <div class="input-group-addon horz-addon">Content</div> 
						<% if @todo_item.errors.any? %>
						<div id="error_explanation">
						  <h2><%= pluralize(@todo_item.errors.count, "error") %> prohibited this todo item from being saved:</h2>

						  <ul>
						    <% @todo_item.errors.full_messages.each do |msg| %>
						      <li><%= msg %></li>
						    <% end %>
						  </ul>
						</div>
						<% end %>
 						 <%= form.text_area :content,  :rows=>"3", :class => "  form-control" %>                       
 						 <div class="input-group-addon horz-addon"></div>
                      </div>
                    </div>
            </div>
               <hr>
            <center>
              <div class="form-footer">
                <button class="btn btn-lg btn-primary create-button" type="submit">Save Item</button> 
 				<%=link_to_function "Cancel", "history.back()",:class => "btn btn-lg btn-danger create-button" %>
                <% end %>  
              </div>
              </center>  
          </form>
            </div> 
</div>